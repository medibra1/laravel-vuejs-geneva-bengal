<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <!-- Test environment variables deliberately live in tests/bootstrap.php
         rather than in a <php><env> block here: an <env> (even with
         force="true") only writes putenv()/$_ENV, which Laravel's Env
         repository ranks *below* the $_SERVER entries docker-compose
         injects into the container. See tests/bootstrap.php. -->
</phpunit>
